{
  "name": "wuzhong-tobacco-service",
  "version": "1.0.0",
  "private": true,
  "description": "吴中零售客户营销服务 - 面向持证卷烟零售客户的业务通知与信息查询工具",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "import-data": "node scripts/import-excel.js",
    "generate-template": "node scripts/generate-template.js",
    "sync": "npm run import-data && git add -A && git commit -m '更新数据' && git push",
    "deploy-cos": "npm run build && node scripts/deploy-cos.js",
    "sync-all": "npm run import-data && git add -A && git commit -m '更新数据' && git push && npm run deploy-cos"
  },
  "dependencies": {
    "cos-nodejs-sdk-v5": "^3.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
