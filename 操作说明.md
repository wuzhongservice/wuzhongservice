# 操作说明

> 本说明面向完全不懂代码的人员，请按照步骤操作。

---

## 第一部分：首次使用

### 1. 安装 Node.js

Node.js 是运行本系统的基础环境。

**操作步骤：**

1. 打开浏览器，访问 https://nodejs.org
2. 下载左侧的 **LTS（长期支持版）**（例如 18.x 或 20.x）
3. 打开下载的文件，一路点击"下一步"完成安装
4. 安装完成后，打开"终端"（Mac）或"命令提示符"（Windows）
5. 输入以下命令确认安装成功：

```
node --version
```

**你应该看到：** 显示 v18.x.x 或 v20.x.x 这样的版本号

---

### 2. 打开项目文件夹

**Mac 操作步骤：**

1. 打开"终端"应用程序（在"启动台" → "其他" → "终端"）
2. 输入以下命令（注意空格）：

```
cd /Users/ltazy/Desktop/workplace/wuzhong-tobacco-service
```

3. 按回车键

**你应该看到：** 命令行前面变成 `wuzhong-tobacco-service` 这个路径

---

### 3. 安装依赖

在终端中（确保已经在项目文件夹内），输入：

```
npm install
```

按回车键。

**你应该看到：** 出现一些文字滚动，最后显示 `added XXX packages`，没有红色报错。

---

### 4. 启动网页

在终端中输入：

```
npm run dev
```

按回车键。

**你应该看到：**

```
VITE v5.x.x  ready in XXX ms
➜  Local:   http://localhost:3000/
```

此时不要关闭这个终端窗口。

**打开网页：**

1. 打开电脑上的浏览器（Chrome、Edge、Safari 均可）
2. 在地址栏输入 `http://localhost:3000/`
3. 按回车键

**你应该看到：** 项目首页已经打开，显示"吴中零售客户营销服务"

---

### 5. 在手机上预览

1. 确保手机和电脑连接在同一个 Wi-Fi 网络
2. 在终端中找到 `Network` 地址，例如 `http://172.20.10.3:3000/`
3. 在手机浏览器中输入该地址

**你应该看到：** 手机上也显示了同样的网页

---

### 6. 停止运行

在终端中按键盘上的 `Ctrl + C`（两个键同时按）。

**你应该看到：** 终端回到可以输入命令的状态。

---

## 第二部分：每周更新

### 1. 打开哪个 Excel 文件

打开项目文件夹中的：

```
data/营销信息.xlsx
```

### 2. 每张表应该填写什么

Excel 中有 8 个工作表（底部选项卡）：

| 工作表 | 填写说明 |
|--------|---------|
| **首页通知** | 本周重要通知，最多3条。标题必填，可写内容。"紧急"列填"是"会高亮显示 |
| **套餐信息** | 每周套餐。套餐名称必填。周次写数字（如30）。日期填写格式见下文 |
| **卷烟信息** | 所有卷烟品种。品规名称和品牌必填。价格先写100.00或"演示价格" |
| **日程安排** | 每天的事项。日期、事项类型、事项标题必填 |
| **直播信息** | 每周四直播。直播主题和直播时间必填。状态可选：即将开始/直播中/已结束/可回看 |
| **制度解读** | 分档制度的通俗解释。标题必填 |
| **应急订单** | 应急订单安排。标题必填。状态可选：待开始/进行中/已结束/已调整 |
| **基础设置** | 网站名称、联系方式等。键名和键值必填。不要删除已有行 |

### 3. 哪些字段不能删除

- 每张表的**第一行**（列标题行）不能删除或改名
- "基础设置"表中的**已有行**不能删除（可以修改"键值"列的内容）
- 每行中打了"必填"标记的列不能留空

### 4. 日期应该怎么填写

日期统一格式：`YYYY-MM-DD`

例如：
- 2026年7月30日 → `2026-07-30`
- 2026年8月1日 → `2026-08-01`

**注意：** 月和日必须两位数字，1月要写01，1日要写01。

如果包含具体时间，写成：`2026-07-30 10:00`

### 5. 如何保存 Excel

编辑完成后，按 `Ctrl + S`（Mac 按 `Command + S`）保存文件。

**注意：** 保存后关闭 Excel 文件（某些情况下打开的文件会被锁定，导致脚本读取失败）。

### 6. 如何执行数据更新

在终端中（确保已经在 wuzhong-tobacco-service 文件夹），输入：

```
npm run import-data
```

按回车键。

**你应该看到：**

```
✅ 数据已写入: public/data/content.json
🎉 导入完成！
```

如果出现红色错误，请查看错误提示中说的是哪个工作表、哪一行、哪个字段的问题，修改 Excel 后重新执行。

### 7. 如何查看更新结果

执行成功后，启动网页查看：

```
npm run dev
```

在浏览器中打开 `http://localhost:3000/`，确认内容已更新。

### 8. 如何重新生成正式文件

确认内容无误后，执行：

```
npm run build
```

**你应该看到：** `✓ built in XXXms`，没有红色报错。

### 9. 更新失败时如何恢复备份

每次执行 `npm run import-data` 时，系统会自动备份上一次的数据到：

```
public/data/backup/content-XXXX.json
```

如果更新后发现问题，可以：
1. 在 `public/data/backup/` 中找到最近的备份文件
2. 将备份文件复制为 `public/data/content.json`（覆盖即可）
3. 或者重新编辑 Excel 修正问题后再次导入

---

## 第三部分：正式打包

### 1. 执行构建命令

内容确认无误后，执行：

```
npm run build
```

### 2. 打包后的文件在哪里

构建好的文件在项目文件夹的 `dist/` 目录中：

```
wuzhong-tobacco-service/dist/
├─ index.html
├─ assets/xxx.js
├─ assets/xxx.css
├─ data/content.json
└─ images/
```

### 3. 需要把哪些文件交给技术人员

将整个 `dist/` 文件夹交给单位的技术人员。

技术人员会把这个文件夹里的所有文件部署到服务器上。

### 4. 部署后客户如何访问

部署完成后，客户可以通过以下方式访问：

- **固定链接**：直接在手机浏览器打开网址
- **二维码**：将网址生成二维码，打印后张贴在店内
- **微信群**：将链接或二维码发送到客户微信群

---

## 第四部分：用 GitHub Pages 免费发布到外网

这个方法最简单，**不需要花钱、不需要单位 IT 配合**，项目在本节操作完成后即可让所有客户通过手机访问。

### 第1步：注册 GitHub 账号

1. 打开浏览器，访问 https://github.com
2. 点右上角的 **"Sign up"**（注册）
3. 输入邮箱，设置密码（至少8位），给自己取一个用户名（例如 `wuzhong-tobacco`）
4. 完成邮箱验证
5. 注册成功后会自动登录

### 第2步：创建仓库

1. 登录 GitHub 后，点击页面右上角的 **"+"** 图标 → **"New repository"**
2. **Repository name** 输入：`wuzhongservice`
3. **Description** 可选填：`吴中零售客户营销服务`
4. **Public** 保持选中（必须选 Public）
5. 不要勾选其他选项
6. 点击绿色的 **"Create repository"** 按钮

### 第3步：获取仓库地址

创建成功后，你会进入一个新页面。找到页面中部的 **"…or push an existing repository from the command line"** 部分，你会看到两行代码：

```bash
git remote add origin https://github.com/你的用户名/wuzhongservice.git
git push -u origin main
```

**复制这两行代码**（不要关闭这个网页）。

### 第4步：在电脑上推送代码

1. 打开终端
2. 输入以下命令进入项目文件夹（逐行输入，每次按回车）：

```bash
cd /Users/ltazy/Desktop/workplace/wuzhong-tobacco-service
```

3. 粘贴刚才复制的**第一行**命令（添加远程仓库），按回车
4. 再粘贴**第二行**命令（推送代码），按回车

**你可能会看到：**
- 弹出一个浏览器窗口让你登录 GitHub——登录即可
- 或者提示输入用户名和密码——输入 GitHub 的用户名和密码

**你应该看到：**
```
 * [new branch]      main -> main
 Branch 'main' set up to track remote branch 'main' from 'origin'.
```

### 第5步：启用 GitHub Pages

1. 回到 GitHub 网页（你刚创建仓库的那个页面）
2. 点顶部的 **"Settings"**（设置）选项卡
3. 在左侧菜单找到 **"Pages"**（页面）
4. 在 **"Branch"** 下面的下拉菜单选择 **"main"**
5. 旁边的文件夹选择 **"/(root)"**
6. 点击 **"Save"**（保存）
7. 等 1-2 分钟后刷新页面，你会在顶部看到一行绿色提示：
   **"Your site is live at https://你的用户名.github.io/wuzhongservice/"**

### 第6步：完成

现在，把这个链接发给客户，或者在百度搜索"在线二维码生成器"，生成二维码打印出来：

```
https://你的用户名.github.io/wuzhongservice/
```

### 以后更新内容

以后每周更新数据时：

```bash
# 1. 修改 data/营销信息.xlsx

# 2. 转换数据
npm run import-data

# 3. 提交到 GitHub（会自动发布更新）
git add -A
git commit -m "更新第XX周数据"
git push
```

提交后等大约 **1-2 分钟**，客户刷新网页就能看到更新内容。

**不需要**再手动执行 `npm run build`，GitHub 会自动构建。

---

## 常见问题

**问：终端报错"command not found"是什么意思？**

答：说明 Node.js 没有安装成功，或者当前不在项目文件夹中。请重新按照"首次使用"的步骤操作。

**问：修改 Excel 后刷新网页没有变化？**

答：需要先执行 `npm run import-data` 更新数据，然后再刷新网页。如果还不行，重新执行 `npm run dev` 启动服务器。

**问：手机无法访问网页？**

答：请确认手机和电脑连接的是同一个 Wi-Fi，并且地址输入正确（用 Network 显示的地址，不是 localhost）。

**问：如何把网页发布到外网？**

答：推荐用 GitHub Pages（免费），详见本说明的第四部分。也可以将 `dist/` 文件夹交给单位的技术人员，部署到单位网站或云服务器。

---

> 如有其他问题，请联系系统开发人员。
